name: Deploy to Render

on:
  push:
    branches: ["Final-Week"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy hook
        run: curl -sS "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
