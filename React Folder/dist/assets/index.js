// I need to export the React app from the index.jsx file so that it can be imported in the index.html file and rendered to the DOM.

const javascriptx = require("../src/index.jsx");

module.exports = {
  javascriptx,
};
